# Copy to .env.local and set a real value before deploying.
#
#   openssl rand -base64 48
#
# In development this can be left unset — a secret is generated once and kept
# in .data/session-secret so logins survive a dev-server restart.
SESSION_SECRET=
